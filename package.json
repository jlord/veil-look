{
  "name": "veil-look",
  "description": "It's a weblog",
  "tags": [
    "eleventy",
    "11ty",
    "ssr",
    "static",
    "glitch",
    "template",
    "webring"
  ],
  "version": "1.0.0",
  "scripts": {
    "build": "eleventy --pathprefix=/veil-look",
    "deploy": "eleventy --pathprefix=/veil-look && http-server dist/",
    "dev": "eleventy --pathprefix=/veil-look --serve",
    "start": "npm run deploy"
  },
  "dependencies": {
    "@11ty/eleventy": "^0.11.0",
    "@11ty/eleventy-plugin-rss": "^1.0.7",
    "http-server": "^0.12.3",
    "node-fetch": "^2.6.7"
  },
  "engines": {
    "node": "12.x"
  },
  "license": "MIT"
}
